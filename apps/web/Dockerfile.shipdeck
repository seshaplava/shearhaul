FROM node:20-bookworm-slim
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev || npm install --omit=dev
COPY . .
RUN npm run build || true
ENV PORT=3002
EXPOSE 3002
CMD ["sh","-c","npx next start --port 3002 --hostname 0.0.0.0"]
