FROM node:20-bookworm-slim
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev || npm install --omit=dev
COPY . .
RUN npm run build || true
ENV PORT=3001
EXPOSE 3001
CMD ["sh","-c","npx next start --port 3001 --hostname 0.0.0.0"]
